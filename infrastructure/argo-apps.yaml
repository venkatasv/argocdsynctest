apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-standalone
  namespace: redis
spec:
  project: gitops
  sources:
    - repoURL: 'https://charts.bitnami.com/bitnami'
      chart: redis
      targetRevision: 18.2.1
      helm:
        valueFiles:
          - '$values/infrastructure/redis-standalone/values.yaml'
    - repoURL: 'https://github.com/venkatasv/argocdsynctest.git'
      targetRevision: HEAD
      path: infrastructure/redis-standalone

  project: gitops
  sources:
    - repoURL: 'https://charts.bitnami.com/bitnami'
      chart: redis
      targetRevision: 18.2.1               # <-- Use exact version (e.g. 18.2.1 instead of 18.x.x)
      helm:
        valueFiles:
          - '$values/infrastructure/redis-standalone/values.yaml'
    - repoURL: 'https://github.com/venkatasv/argocdsynctest.git'
      targetRevision: HEAD
      path: infrastructure/redis-standalone
>>>>>>> d35cada (Add ArgoCD AppProject to allow Bitnami/helm repos; update Applications to use 'gitops' project; update workflow to apply project before apps)
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: redis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
<<<<<<< HEAD
=======
      - CreateNamespace=true                # <-- Auto-creates the 'redis' namespace if missing


